{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Pytest",
            "type": "debugpy",
            "request": "launch",
            "module": "pytest",
            "cwd": "${workspaceFolder}/apicultura",          
            "pythonArgs": [ "-Xfrozen_modules=off"],
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}